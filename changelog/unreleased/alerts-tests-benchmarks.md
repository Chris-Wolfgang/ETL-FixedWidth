type: internal

Tests/benchmarks: two duplicate tests merged into their twins (S4144), the fuzz `Drain` helper awaits the enumeration once (S5034), and `BenchmarkScratch` carries a justified S5443 suppression.
